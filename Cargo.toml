[package]
name = "graph1_minifb_demo"
version = "0.1.0"
edition = "2021"


[dependencies]
minifb = "0.28.0"
#graph1 = { path = "../graph1", features = ["gpu"] }
graph1 = { path = "../graph1" }
#graph1 = { path = "../graph1" }
graph1_wasm_demo = { path = "../graph1_wasm_demo" }


[profile.release]
debug = false
#opt-level = 'z' # or 's' for size optimization with a balance between size and speed
opt-level = 3 # or 's' for size optimization with a balance between size and speed
lto=true

